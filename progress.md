
Task 8: implemented, DONE, commit edd25e5 (BASE 6cdba11). Diff limited to the four authorized files: index.html (modal.js script tag), js/modal.js (new), js/app.js (defect 2), js/diagram3d.js (defect 1).
  - Defect 1 fixed as ruled: `resize()` now sizes from `canvas.parentElement || container`, so it follows the re-parent with no new API. Verified in the on-disk file.
  - Defect 2 fixed: container-agnostic svg lookup, so "2D" inside an open modal works.
  - Defect 3 (`overflow:hidden` label clipping): implementer INVESTIGATED rather than assumed, and reached a different conclusion than the hypothesis it was handed. It measured the clipping reproducing IDENTICALLY in the thumbnail with the modal closed, which rules out re-parenting and the modal as the cause. Diagnosis: pre-existing fixed 45° vertical FOV camera framing from Task 5, clipping `Ec`, `Fee`, `Uh`. Correctly did not touch css/style.css (outside its file list) and recommended fitting the camera to the model's bounding box instead of loosening `overflow`. This is the right shape of answer — it distinguishes symptom from cause.
  - Verification: 10 checkpoints, `document.querySelectorAll('canvas').length === 1` at every one — the single-WebGL-context invariant holds across open/close/toggle. Orbit-drag re-tested with the modal genuinely open after an advisor correction (the first attempt had tested it in the wrong state). One anomaly during testing was diagnosed as a stale ES-module cache on a pre-existing browser tab by cross-checking the http.server access log, not hand-waved as flakiness.
  - **New ruling — camera-fit fix scheduled before Task 9.** The label clipping is on the demo's centerpiece and is visible at DEFAULT params in the thumbnail, i.e. the first thing anyone looking at this portfolio piece sees. Not deferring it to final-review triage. Scope: fit the camera to the model's bounding box in js/diagram3d.js so labels stay inside the viewport across the plausible param range, INCLUDING large-H1 cases. Deliberately not loosening `.diagram-container{overflow:hidden}` — that hides the symptom, lets labels escape the panel and overlap the surrounding console chrome, and would look worse. Cost if wrong: a camera that re-frames on every param change could feel jumpy — mitigated by requiring verification at default AND extreme params, and by fitting on rebuild rather than per frame.
Task 8 review dispatched over review-6cdba11..edd25e5.diff, with the three known deferrals declared as non-findings and with explicit adversarial questions attached: orphan/duplicate node sequences (open-in-3D, close-in-3D, double-click expand, backdrop click landing on a child), the display:none-during-resize zero-size ordering hazard in BOTH directions, whether OrbitControls' canvas-bound listeners survive re-parenting, and whether reading `window.__courtierApp` instead of importing app.js is actually timing-safe given the script tag order. Also asked it to independently assess the defect-3 camera-framing diagnosis, since a fix is being scheduled off the back of it.

Task 8 review verdict: spec ✅, quality **Approved** — 0 Critical, 0 Important, 0 Minor. Cleanest review of the run. Reviewer verified empirically rather than trusting the report, and answered every adversarial question I posed:
  - Defect 1 confirmed real: measured `canvas.width/height` immediately after `openModal()` as exactly 852x450, matching the modal container — no zero-size frame, because `classList.remove('hidden')` runs BEFORE `moveDiagramInto()` reads dimensions. Confirmed the reverse order is safe too (the thumbnail container's visibility never depended on the modal's hidden class). Confirmed neither branch can yield zero or NaN.
  - Defect 2 confirmed real: reproduced the exact original failure mode and its fix — opened the modal, switched to 3D, switched back to 2D while still open, and read `svgParent: "modal-diagram-container"`, `svgDisplay: "block"`. Also confirmed the compound selector cannot match the wrong svg: there is exactly ONE `<svg>` in the app, created once by `mountDiagram2D` and moved rather than cloned.
  - Backdrop click: verified a click whose target is the inner `.modal` does NOT close (the `e.target === overlayEl` guard holds), while a click on the overlay itself does. No accidental-close bug.
  - Double-open idempotency: `appendChild` on nodes already in the target container is a harmless no-op reorder, so double-clicking "Agrandir" cannot duplicate or orphan nodes.
  - OrbitControls survives re-parenting by construction — bound to the canvas DOM node, not the container, and the canvas is moved, never destroyed/recreated.
  - `window.__courtierApp` timing is safe: both scripts are non-async `type="module"`, which execute in document order with defer semantics after all transitive imports resolve, so app.js's final assignment completes before modal.js's top-level code regardless of CDN latency — and modal.js only reads it lazily inside click handlers anyway.
  - **Independently confirmed the defect-3 camera diagnosis by measurement**, which matters because a fix was being scheduled on it: measured every clipped label's `getBoundingClientRect()` relative to its container, after allowing a real animation frame to elapse, in BOTH sizes. Thumbnail (288x220): `Ec` top ≈ -15.8px, `Fee` ≈ -36.3px. Modal (852x450): `Ec` ≈ -23.9px, `Fee` ≈ -65.9px. Negative in both, and `PerspectiveCamera(45, ...)`'s 45 is the VERTICAL fov, independent of aspect — so the same model points fall outside the frustum at any container size. That is frustum under-framing, not container clipping. Agreed that flipping to `overflow: visible` would only spill labels over the layout without addressing the under-framing.
Task 8: complete (commits 6cdba11..edd25e5, review clean, 0 fix rounds).

Camera-fit fix: dispatched (sonnet). BASE edd25e5. Scope: js/diagram3d.js only. Handed the root cause as ESTABLISHED (two independent measurements agree) so the agent spends its effort on the fix, not on re-deriving the diagnosis. Requirements specified: derive framing from `Box3().setFromObject(dynamicGroup)` not hardcoded numbers; account for horizontal extent too, not just the vertical fov; add justified margin because the labels sit OUTSIDE the geometry bounding box (offset by `offsetDir * 0.05` in world space plus their own DOM pixel size), so a box-tight fit still clips; re-fit in `resize()` as well as on geometry rebuild since aspect changes at runtime between the 288x220 thumbnail and the 852x450 modal.
  - Explicit anti-requirement: do NOT re-fit in the rAF loop — that would snap the camera back on every frame and make OrbitControls unusable. Asked it to decide and JUSTIFY whether a user's manual orbit should survive a param change.
  - Verification demands measurement, not eyeballing: every label div's `getBoundingClientRect()` asserted inside its container on all four edges, after a real animation frame, across 5 states — thumbnail default, modal default, modal H1=1.5 (tall), modal H1=0.05 (short, to catch over-zoom/inversion), and post-orbit-drag to confirm no snap-back. Told explicitly NOT to quietly commit a half-fix: if a state fails, report which and by how much.

Camera-fit fix: DONE, commit 63dcb1c (BASE edd25e5). js/diagram3d.js only, +83 lines.
  - Implementation: `computeFit()`/`fitCameraToBox()` derive framing from the model's Box3 via its BOUNDING SPHERE (orientation-independent, so an orbit cannot change the required distance), accounting for both the vertical fov and the aspect-derived horizontal fov. `FIT_PADDING = 1.3`, justified rather than tuned by eye: it covers the labels' 0.05-world-unit `offsetDir` displacement plus the label DOM elements' own pixel footprint, both of which sit OUTSIDE the geometry bounding box a tight fit would frame.
  - Re-fit policy: runs in `buildScene()` (geometry rebuild) and `resize()` (container/aspect change), never in the rAF loop. Preserves the user's current orbit DIRECTION instead of resetting to the default view, so a manual orbit survives both a param change and a modal open/close. Right call — snapping the camera back on every keystroke would make the form unusable while orbited.
  - Verification by measurement, as demanded: all 11 labels fully inside their container on all four edges in all 5 states (thumbnail default, modal default, modal H1=1.5, modal H1=0.05, post-orbit-drag + subsequent param change). The H1=0.05 case confirms no over-zoom or inversion at tiny models. Orbit non-snap-back confirmed with before/during/after screenshots. 2D toggle and modal open/close still work.
  - Only console message across the whole session was the pre-existing favicon.ico 404 — which Task 9 is now fixing.

Task 9: dispatched (sonnet). BASE 63dcb1c. Final task. Brief extended with four carried rulings: (1) use and VERIFY `node --test tests/*.mjs`, overriding the plan's and brief's `node --test tests/` which fails MODULE_NOT_FOUND on this machine's Node v22.16.0 — with an explicit instruction not to document a command it has not executed; (2) amend spec §8 to describe the as-built `window.__courtierApp` view-mode ownership rather than the never-built `state.js` version, WITHOUT changing code, after reading app.js and modal.js so the wording matches reality; (3) add an inline `data:` URI favicon to kill the last console 404, without otherwise touching the head importmap; (4) refresh spec §10's checklist for the four things fixed after the spec was written (2D viewBox derivation, WebGL disposal, camera bounding-box fit, canvas re-parenting) so the spec does not ship describing a superseded implementation.
  - README content requirements specified so it is a real deliverable rather than boilerplate: what it demonstrates, the `python3 -m http.server` run command AND why file:// fails (ES module CORS), the verified test command, the architecture rationale (shared `dimensionRecords` as the single source of truth preventing 2D/3D drift; pub-sub store; single-WebGL-context re-parenting), and an honest "what this is not" section restating spec §2's non-goals — dimensions are cosmetic, not validated against any engineering standard. Told not to oversell.

Session cost note: ~$97 at Task 9 dispatch. Informational; user has not asked to economize. The scope warning about "22 files modified" is expected — it counts the SDD workspace briefs/reports/diffs, not product files. Product files are index.html, css/style.css, js/*.js (6), tests/*.mjs (2), plus docs.
